<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.malhandocerto.malhandocerto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Abdomen" />
        <activity android:name=".Biceps" />
        <activity android:name=".Costas" />
        <activity android:name=".Ombro" />
        <activity android:name=".Perna" />
        <activity android:name=".Peito"></activity>
        <activity android:name=".abertura"></activity>
    </application>

</manifest>
